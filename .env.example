# ============================================
# 小智 MQTT 网关环境变量配置示例
# ============================================

# 【必须配置】服务器 IP 或域名
# 用于告知设备 UDP 服务器的地址
#
# 本地部署示例：
#   - 局域网 IP：192.168.1.100（常见于家庭网络）
#   - 局域网 IP：10.0.0.100（常见于办公网络）
#   - Docker Desktop (Mac)：host.docker.internal
#   - 获取本地IP：在终端运行 ipconfig (Windows) 或 ifconfig (Mac/Linux)
#
# 公网部署示例：
#   - 域名：mqtt.xiaozhi.me
#   - 公网IP：1.2.3.4
#
PUBLIC_IP=192.168.1.100

# 【必须配置】API 签名密钥
# 用于生成每日访问令牌，保护管理 API 接口
# 要求：至少8位，包含大小写字母，不能包含弱密码（test、1234、admin、password 等）
# 示例：StrongKey2024
MQTT_SIGNATURE_KEY=

# ============================================
# 以下为可选配置（有默认值）
# ============================================

# MQTT TCP 端口（默认：1883）
MQTT_PORT=1883

# UDP 端口（默认：8884）
UDP_PORT=8884

# 管理 API 端口（默认：8007）
API_PORT=8007
